import { AddEditDirDirective } from './add-edit-dir.directive';

describe('AddEditDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AddEditDirDirective();
    expect(directive).toBeTruthy();
  });
});
